<%= link_to gravatar_for(object, size: 52), user_path(object) %>
<h1><%= object.name %></h1>
<span><%= link_to "view my profile", user_path(object) %></span>
<span><%= pluralize(object.tweets.count, "tweet") %></span>